{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "..",
    "types": ["node", "vitest"]
  },
  "include": ["../src/**/*.ts", "../vitest.config.ts", "./**/*.ts"],
  "exclude": ["../node_modules", "../dist"]
}
